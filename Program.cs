using System;

namespace Monogame_Animation_PHILIP_GRAHAM
{
    public static class Program
    {
        [STAThread]
        static void Main()
        {
            using (var game = new Game1())
                game.Run();
        }
    }
}
